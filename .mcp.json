{
  "mcpServers": {
    "angular-cli": {
      "command": "npx",
      "args": ["-p", "@angular/cli@21", "ng", "mcp"],
      "env": {
        "npm_config_registry": "https://registry.npmjs.org"
      }
    }
  }
}
